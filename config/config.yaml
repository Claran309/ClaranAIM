openai:
  api_key: "your_api_key"
  base_url: "your_base_url"
  model_name: "your_model_name"

storage:
  session_dir: "agent_sessions_dir"

agent:
  root: "agent_root_dir"

cozeloop:
  api_token: "your_cozeloop_api_token"
  workspace_id: "your_cozeloop_workspace_id"